import React from 'react';
import MaterialDashboard from './pages/MaterialDashboard';

/**
 * Aplicación principal para la gestión de materiales
 * Punto de entrada del módulo de materiales que renderiza la lista completa
 * @returns {JSX.Element} Componente principal de materiales
 */
const MaterialDashboardApp = () => {
  return <MaterialDashboard />;
};

export default MaterialDashboardApp;
    